{% block form_errors %}
    {% spaceless %}
        {% if errors|length > 0 %}
            {% if compound %}

                    {% for error in errors %}
                        {{ error.message }}
                    {% endfor %}

            {% else %}
                {# ... display the errors for a single field #}
            {% endif %}
        {% endif %}
    {% endspaceless %}
{% endblock form_errors %}